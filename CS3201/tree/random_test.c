#include <stdio.h>
#include <stdbool.h>


int main(int argc, char *argv[])
{ 
  int a;
  bool b;
  b=10;
  printf("\n The size of int is %d  \n", sizeof(a));
  printf("\n The size of bool is %d, And the value is %d  \n", sizeof(++b), b);
  return 0;
}
